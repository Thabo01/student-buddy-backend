package com.studentbuddy.model;

/**
 * Lifecycle states for a task. These map directly to the Kanban columns on the
 * frontend (To Do / In Progress / Done).
 */
public enum TaskStatus {
    TODO,
    IN_PROGRESS,
    DONE
}
