package com.studentbuddy.model;

/**
 * Severity of a notification. Values line up with Bootstrap alert styles
 * (info / warning / danger) so the frontend can render them directly.
 */
public enum NotificationSeverity {
    INFO,
    WARNING,
    DANGER
}
